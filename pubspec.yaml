name: dropdownpro
description: "A customizable dropdown overlay package for Flutter. #dropdown #dropdownpro"
version: 0.0.1-dev.1
homepage: https://github.com/PravinKunnure/dropdownpro
repository: https://github.com/PravinKunnure/dropdownpro

environment:
  sdk: ">=3.10.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # Uncomment and add any packages your dropdownpro uses
  # dio: ^6.0.0
  # flutter_dotenv: ^5.0.2
  # uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
# Assets can go here if your package needs them
# assets:
#   - assets/images/

